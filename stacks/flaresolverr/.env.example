# FlareSolverr env vars
# No secrets required — all config via docker-compose environment

# These must be set in Portainer UI for every stack:
# DOCKER_DATA_HOME=/path/to/docker/data
# DOCKER_SHARED_HOME=/path/to/shared/data
